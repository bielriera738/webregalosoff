---
import Layout from "../layouts/Layout.astro";
import Hero from "../components/Hero.astro";
import ProductCard from "../components/ProductCard.astro";
import { searchProducts } from "../services/amazon";

const pageTitle =
    "Regalos Originales para Madres 2024 | Sorprende a Mamá - Regaloteca";
const pageDescription =
    "Descubre nuestra selección de los 20 mejores regalos originales para madres. Desde tecnología hasta bienestar, encuentra el detalle perfecto para sorprenderla.";

const products = await searchProducts("gifts for mother");
---

<Layout title={pageTitle} description={pageDescription}>
    <Hero
        title="Regalos Originales para Madres"
        subtitle="Porque ella se merece lo mejor: ideas únicas para sorprender a la mujer más importante de tu vida."
    />

    <section class="container mb-lg">
        <div class="intro-text">
            <h2>Encuentra el Regalo Perfecto para Mamá</h2>
            <p>
                Sabemos que encontrar el regalo ideal para una madre no siempre
                es tarea fácil. Ellas suelen decir que "no necesitan nada", pero
                en el fondo, a todas les encanta sentirse mimadas y apreciadas.
                En <strong>Regaloteca</strong>, hemos curado una lista exclusiva
                con los <strong
                    >20 regalos más originales, útiles y valorados</strong
                > que puedes encontrar en Amazon.
            </p>
            <p>
                Ya sea para el Día de la Madre, su cumpleaños, Navidad o
                simplemente porque sí, aquí encontrarás opciones para todos los
                gustos: desde gadgets tecnológicos que le facilitarán la vida,
                hasta kits de relajación para que disfrute de su merecido
                descanso. ¡Explora nuestra selección y hazla sonreír!
            </p>
        </div>
    </section>

    <section class="container mb-xl">
        <div class="grid-cols-4">
            {products.map((product) => <ProductCard product={product} />)}
        </div>
    </section>

    <section class="bg-light py-lg">
        <div class="container">
            <h2 class="text-center mb-md">¿Por qué elegir estos regalos?</h2>
            <div class="features-grid">
                <div class="feature">
                    <h3>⭐ Calidad Garantizada</h3>
                    <p>
                        Seleccionamos solo productos con altas valoraciones y
                        excelentes reseñas de otros usuarios.
                    </p>
                </div>
                <div class="feature">
                    <h3>🚚 Envío Rápido</h3>
                    <p>
                        Al comprar a través de Amazon, disfrutas de la rapidez y
                        seguridad en el envío que ya conoces.
                    </p>
                </div>
                <div class="feature">
                    <h3>🎁 Originalidad</h3>
                    <p>
                        Buscamos ideas que se salen de lo común para que tu
                        regalo sea verdaderamente memorable.
                    </p>
                </div>
            </div>
        </div>
    </section>
</Layout>

<style>
    .intro-text {
        max-width: 800px;
        margin: 0 auto;
        text-align: center;
        font-size: 1.1rem;
        color: var(--color-text-muted);
    }

    .intro-text h2 {
        margin-bottom: var(--spacing-sm);
        color: var(--color-secondary);
    }

    .intro-text p {
        margin-bottom: var(--spacing-sm);
    }

    .bg-light {
        background-color: white;
        padding-top: var(--spacing-lg);
        padding-bottom: var(--spacing-lg);
    }

    .features-grid {
        display: grid;
        grid-template-columns: repeat(auto-fit, minmax(250px, 1fr));
        gap: var(--spacing-md);
        text-align: center;
    }

    .feature h3 {
        color: var(--color-primary);
        margin-bottom: var(--spacing-xs);
    }
</style>
